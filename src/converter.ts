import type{TraceFailure,EvalCase}from"./types.js";
export function failuresToEvals(failures:TraceFailure[],sessionId?:string):EvalCase[]{return failures.filter(f=>!sessionId||f.sessionId===sessionId).map((f,i)=>{const tool=f.event.tool||"unknown";const error=f.event.error||"unexpected";const sev=error.includes("timeout")?"critical":(f.event.durationMs&&f.event.durationMs>5000)?"high":"medium";return{name:`regression_${tool}_${i+1}`,description:`Auto-generated from trace: ${error.slice(0,80)}`,input:f.event.input||{},expectedBehavior:`Handle '${tool}' without: ${error.slice(0,50)}`,severity:sev};});}